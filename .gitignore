# Local Claude Code settings
.claude/settings.local.json

# impeccable design skill (dev tooling, not part of the extension)
.claude/skills/
.agents/

# OS files
.DS_Store
Thumbs.db

# Build output
*.zip

# impeccable critique snapshots (internal design notes)
.impeccable/

# Secrets and local tool config — never commit (defense in depth).
# Targeted patterns only; the tracked manifest.json / manifest.firefox.json
# are intentionally NOT matched here.
.env
.env.*
*.pem
*.key
**/mcp.json
**/.mcp.json
**/.claude.json
.claude/settings.json
